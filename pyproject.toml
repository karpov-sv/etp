[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "etp"
version = "0.1.0"
description = "ETPathfinder daemon framework for networking services."
requires-python = ">=3.8"

[tool.setuptools]
packages = ["etp"]
